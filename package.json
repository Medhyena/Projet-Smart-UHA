{
  "name": "myrestnodejsapp",
  "version": "0.0.1",
  "description": "nodejs app that send and receive data to an optimization algorithm",
  "main": "OptiTest.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chackal",
  "dependencies": {
    "express": "^4.16.4"
  }
}
